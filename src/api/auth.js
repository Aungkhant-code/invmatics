// api/auth.js
// API calls for authentication and staff management
// - login(email, password) — returns JWT token
// - logout()
// - getMe() — current logged-in user and role
// - getStaff()
// - createStaff(data)
// - updateStaff(id, data)
// - deactivateStaff(id)
// - getActivityLog(filters)
