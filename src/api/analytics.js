// api/analytics.js
// API calls for dashboard and reports
// - getDashboardSummary()
// - getReport(type, filters) — stock, movement, sales, margin, customers, suppliers
// - exportReportExcel(type, filters) — triggers .xlsx download
// - exportReportPDF(type, filters) — triggers PDF download
