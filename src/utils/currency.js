// utils/currency.js
// Currency formatting and tax helpers
// - formatCurrency(amount, currencyCode) — format with symbol and thousand separators (default USD)
// - calcTax(subtotal, rate) — tax amount at given rate
// - calcTotal(subtotal, rate) — subtotal + tax
// - calcGrossMargin(cost, price) — margin amount and percentage
