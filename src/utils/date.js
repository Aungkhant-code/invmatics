// utils/date.js
// Date formatting helpers
// - formatDate(date) — display format (DD/MM/YYYY)
// - formatDateTime(date) — with time (DD/MM/YYYY HH:mm)
// - isOverdue(dueDate) — returns true if date is before today
// - daysBetween(a, b) — used for lead time calculations
