# config.py
# Application configuration and environment variable management
# - Database connection settings
# - JWT / session secret keys
# - Email / SMTP settings for alert notifications
# - File storage paths (invoices, product images)
# - Tax rate configuration (per-region / per-customer overrides)
# - Accepted payment methods configuration
# - Environment-specific settings (dev, staging, production)
